{
  "name": "hono",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "docker build -t hono-app .",
    "deploy": "bun run scripts/deploy.ts"
  },
  "dependencies": {
    "hono": "^4.6.5"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
